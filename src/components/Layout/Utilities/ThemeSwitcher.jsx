// ThemeSwitcher component removed since we're using light mode only
// This component is no longer needed but kept as placeholder to avoid breaking imports

function ThemeSwitcher() {
  return null; // Component disabled for light-mode-only design
}

export default ThemeSwitcher;
